# 📱 Настройка отправки SMS через Mobizon

## 🔑 Получение API ключа

1. **Зарегистрируйтесь на Mobizon**
   - Перейдите на https://mobizon.kz/
   - Создайте аккаунт

2. **Пополните баланс**
   - Войдите в личный кабинет
   - Пополните баланс (минимум ~500 тенге для тестирования)
   - Стоимость SMS: ~3-5 тенге за сообщение

3. **Создайте API ключ**
   - В личном кабинете найдите раздел "API"
   - Создайте новый API ключ
   - Скопируйте ключ

## ⚙️ Настройка в проекте

### На сервере

Откройте `.env` файл:
```bash
cd /home/ubuntu/memoir/backend
nano .env
```

Добавьте/измените следующие строки:
```env
# SMS Provider - Mobizon
MOBIZON_API_KEY=ваш-реальный-api-ключ-mobizon
SMS_TEST_MODE=False
```

**Важно:**
- `SMS_TEST_MODE=False` - отправлять реальные SMS
- `SMS_TEST_MODE=True` - только логировать коды (для разработки)

### Перезапустите Docker

```bash
cd /home/ubuntu/memoir/backend
sudo docker compose restart backend
```

## 🧪 Тестирование

### Проверка баланса

```bash
curl http://194.32.140.80:8000/api/v1/sms-auth/check-balance
```

Ответ:
```json
{
  "success": true,
  "balance": 1500.50,
  "currency": "KZT",
  "status": "SMS service is operational"
}
```

### Отправка тестового SMS

```bash
curl -X POST http://194.32.140.80:8000/api/v1/sms-auth/send-code \
  -H "Content-Type: application/json" \
  -d '{"phone_number": "+77001234567"}'
```

Ответ при успехе:
```json
{
  "success": true,
  "message": "Verification code sent",
  "sms_id": "12345678",
  "expires_in": 300
}
```

## 📊 Мониторинг

### Логи отправки SMS

```bash
sudo docker compose logs -f backend | grep MOBIZON
```

Вы увидите:
- `📱 [MOBIZON] Sending SMS to...` - начало отправки
- `✅ [MOBIZON] SMS sent successfully` - успешная отправка
- `❌ [MOBIZON] Failed to send SMS` - ошибка отправки

### Логи проверки кодов

```bash
sudo docker compose logs -f backend | grep SMS_VERIFY
```

## 🚨 Возможные проблемы

### Проблема: SMS не отправляются

**Решение:**
1. Проверьте баланс: `curl .../check-balance`
2. Проверьте API ключ в `.env`
3. Убедитесь что `SMS_TEST_MODE=False`

### Проблема: "Invalid API key"

**Решение:**
1. Проверьте правильность API ключа
2. Убедитесь что ключ активен в личном кабинете Mobizon

### Проблема: "Insufficient balance"

**Решение:**
1. Пополните баланс на Mobizon
2. Минимальная сумма: ~100 тенге

### Проблема: SMS приходят медленно

**Решение:**
- Обычное время доставки: 5-30 секунд
- Если дольше - проверьте статус у Mobizon

## 🔄 Переключение режимов

### Для разработки (тестовый режим)

```env
SMS_TEST_MODE=True
```

Коды будут только логироваться:
```bash
sudo docker compose logs backend | grep "📝 \[MOBIZON\]"
```

### Для продакшн (реальные SMS)

```env
SMS_TEST_MODE=False
MOBIZON_API_KEY=ваш-реальный-ключ
```

## 📞 Формат номеров

Mobizon принимает номера в международном формате **без знака +**:

✅ Правильно:
- `77001234567` (Казахстан)
- `79991234567` (Россия)

❌ Неправильно:
- `+77001234567` (будет автоматически очищен)
- `87001234567` (не международный формат)

## 💰 Стоимость

- SMS в Казахстане: ~3-5 тенге
- Минимальное пополнение: 500 тенге
- Примерно 100-150 SMS на 500 тенге

## 📚 Документация

- Mobizon API: https://mobizon.kz/help/api-docs/message
- Личный кабинет: https://mobizon.kz/account/
- Поддержка: support@mobizon.kz
