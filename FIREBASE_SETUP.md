# Настройка Firebase Credentials

## ✅ Файл добавлен

Файл `firebase-credentials.json` успешно добавлен в `backend/` директорию.

## 🔒 Безопасность

- ✅ Файл добавлен в `.gitignore` - не будет попадать в git
- ⚠️ **Важно:** Никогда не коммитьте этот файл в репозиторий!

## 📋 Проверка

### 1. Файл на месте
```bash
ls -la backend/firebase-credentials.json
```

### 2. Файл доступен в контейнере
```bash
docker-compose exec backend ls -la /app/firebase-credentials.json
```

### 3. Путь в .env
Убедитесь, что в `.env` установлен:
```env
FIREBASE_CREDENTIALS_PATH=/app/firebase-credentials.json
```

### 4. Проверка инициализации
После перезапуска контейнеров проверьте логи:
```bash
docker-compose logs celery_beat | grep Firebase
```

Должно быть:
- ✅ `Firebase Admin SDK initialized` (если путь правильный)
- ❌ `FIREBASE_CREDENTIALS_PATH not set` (если путь не установлен)

## 🚀 Для продакшена

1. Скопируйте файл на сервер:
```bash
scp backend/firebase-credentials.json user@server:/path/to/backend/
```

2. Убедитесь, что в `.env` на сервере установлен правильный путь:
```env
FIREBASE_CREDENTIALS_PATH=/app/firebase-credentials.json
```

3. Перезапустите контейнеры:
```bash
docker-compose restart backend celery_worker celery_beat
```

## ✅ Готово!

Теперь уведомления должны работать. Проверьте логи, чтобы убедиться, что Firebase инициализирован.
